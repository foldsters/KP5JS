{
  "name": "kp5js",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild:kotlin": "tsx scripts/prebuild.ts",
    "dev": "npm run prebuild:kotlin && cd .kotlin-build && ./gradlew build && cd .. && concurrently \"./.kotlin-build/gradlew -p .kotlin-build build --continuous -q\" \"vite\"",
    "build": "npm run prebuild:kotlin && ./.kotlin-build/gradlew -p .kotlin-build build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "p5": "^1.4.1",
    "createloop": "^0.0.12",
    "open-simplex-noise": "^2.5.0"
  }
}
